import Link from "next/link";

export default function Settlement() {
  return <main className="mx-auto max-w-[980px] px-6 py-12 w-full"><div className="kicker mb-3">Settlement architecture</div><h1 className="display-hero text-4xl md:text-6xl">One payment rail.<br />No fake balance.</h1><div className="grid md:grid-cols-3 gap-4 mt-10">{[{ n: "01", t: "CHALLENGE", b: "The endpoint returns HTTP 402 with a PAYMENT-REQUIRED header describing price, network, asset, and payTo." }, { n: "02", t: "SIGN + RETRY", b: "The agent applies its own allowlist and spend policy, signs the payment payload, and retries with PAYMENT-SIGNATURE." }, { n: "03", t: "SETTLE", b: "The facilitator verifies and settles; the resource returns with PAYMENT-RESPONSE and the invocation is written to D1." }].map((item) => <div className="panel p-5" key={item.n}><div className="mono text-4xl font-bold text-violet">{item.n}</div><h2 className="font-bold mt-4">{item.t}</h2><p className="text-sm text-dim leading-relaxed mt-2">{item.b}</p></div>)}</div><div className="panel p-6 mt-4"><b>Marketplace split is deliberately not an unaudited smart contract.</b><p className="text-sm text-dim leading-relaxed mt-3">The first production flow should settle to the platform treasury and keep creator obligations in a conventional ledger. The site does not claim automatic creator payout until that process exists and has been reconciled.</p><div className="flex flex-wrap gap-3 mt-5"><Link href="/agents" className="btn-ink">CONNECT AN AGENT</Link><Link href="/console" className="btn-outline">VIEW SETTLED ACTIVITY</Link></div></div></main>;
}
